# AUDIT LOG — Task 1: Stabilizing the Existing Codebase
**Team Members:**  
1. Harshavardini Gowrisankar -10012956
2. Anshita Ramayanam - 10013473 


| Commit SHA | Commit Message | File & Lines | Root Cause/Problem | Change Introduced | How was this discovered |
|---|---|---|---|---|---|
| `10904bc` | Initial commit | code/*.py (all files) | — | Cloned the recovered volatile codebase (data.py, models.py, train.py, fit.py) from the forensic team's GitHub template. Set up local environment, installed dependencies, downloaded and verified the dataset .pt files. | Set up the environment: cloned the repo, made a new venv, installed everything from requirements.txt. |
| `888d74f` | Fix data file path in get_loaders function by removing '_data' suffix | code/data.py (line 10) | The code was building the file path assuming it ended in _data, but none of the actual .pt files were named that way. | Removed the _data suffix from the constructed path so get_loaders correctly resolves {dataset}.pt. | Tried loading a dataset and it just couldn't find the file. Went and checked what the files were actually called, and they didn't have _data in them at all. |
| `894c829` | Update train data slicing for correct validation split in get_loaders function | code/data.py (lines 15-18) | The way the train/validation split was sliced looked off — there was a real risk of the two sets overlapping or the split not landing where it should. | Corrected slicing so train_data = images[:val_start] and val_data = images[val_start:], with matching label slices, ensuring a clean non-overlapping split. | Went through the data loading step by step to make sure train and validation weren't secretly sharing samples, since that's the kind of thing that quietly makes your results look better than they should. |
| `a54f89a, 84fcdfb` | Remove hardcoded global activation string from models.py; update ResNet18 constructor to include activation_str parameter | code/models.py (top-level; ResNet18.__init__) | There was one activation function hardcoded at the top of the file for every single model — no way to swap it out per model or per run. | Removed the global activation_str variable; activation is now passed explicitly as a constructor parameter to models that support it. Added activation_str as an explicit parameter to ResNet18.__init__, resolved via getattr(nn, activation_str) and passed into each ResBlock. | Noticed the activation was just sitting there fixed at the top of the file. Couldn't change it without editing the source directly, which defeats the point of having a config. |
| `a5d6a58` | Fix channel flow in VGGBlock by updating current_in_channels after convolution | code/models.py, VGGBlock class | Inside a VGG block, the channel count wasn't updating between convolutions, so anything with more than one conv layer in the block would break. | Updated current_in_channels = out_channels after each conv layer in the block loop. | Hit a channel mismatch error building a block with more than one conv layer. Went through the block layer by layer to see where the channels stopped lining up. |
| `97ff6c0` | Fix padding configuration for 1x1 convolutions in VGGBlock to maintain spatial dimensions | code/models.py, VGGBlock class | Some of the convolutions in the VGG blocks use a 1x1 kernel, but they were still getting the same padding as the 3x3 ones, which grows the image instead of keeping it the same size. | Set padding=0 specifically when kernel_size == 1, preserving correct spatial dimensions through the block. | Worked out by hand what size the feature map should be after each block, and it didn't match what the code was actually giving. Traced it down to the padding on the 1x1 convs. |
| `2191cb9` | Update AlexNet constructor to accept in_channels parameter for flexibility | code/models.py, AlexNet.__init__ | The first layer of AlexNet only accepted 3 channels, which meant it would break on grayscale data like chest. | Added in_channels as an explicit constructor parameter, used directly in the first nn.Conv2d. | Realized while getting ready to run AlexNet on the grayscale datasets that it would just crash — the first conv layer only knew how to take 3-channel images. |
| `08385b0` | Fix shape mismatch in AlexNet classifier changing to 3072 | code/models.py, AlexNet.classifier | The first linear layer in the classifier expected a different input size than what was actually coming out of the conv layers. | Recomputed the correct flattened feature size and updated nn.Linear to accept 3072 input features. | AlexNet's last conv block puts out 192 channels at a 4x4 size — 192 x 4 x 4 = 3072 — so that's what the linear layer needed to expect. |
| `cef27f1` | fix forward pass return | code/models.py, ResNet18.forward | The forward function did all the work but never actually returned the output, so every call just gave back None. | Added return self.classifier(out) at the end of the forward method. | Ran a forward pass through ResNet18 and got an error about None where a tensor should've been. Went to the forward function and saw it just... ended, no return. |
| `150939c` | Fix optimizer zero_grad placement in training loop | code/fit.py, Trainer.train_one_epoch | Gradients from earlier batches weren't getting cleared at the right point, so they could pile up over the course of training. | Positioned self.optimizer.zero_grad() to execute immediately before loss.backward() on every batch. | Loss wasn't going down the way it should've on an early test run. Checked the training loop and the gradients weren't being reset when they should've been. |
| `a57f961` | Fix variable name from 'sum' to 'total' in training loop to avoid conflict with built-in function | code/fit.py, Trainer.train_one_epoch | A variable was called sum, which shadows Python's actual sum() function — not broken yet, but a bug waiting to happen. | Renamed accumulator variable to total. | Just came across it while reading through fit.py — nothing was failing, but it's the kind of thing that's easy to overlook and annoying to debug later. |
| `e17feb9` | Fix drop_rate parameter in model initialization to use config value | code/train.py | The dropout rate wasn't actually being pulled from the config file when the model got built, so there was no way to tune it per run. | Passed drop_rate=config["DROP"] explicitly into the model constructor call. | Went through train.py looking for values that should've been configurable but weren't hooked up, and dropout was one of them. |
| `9ef61a7` | Update model initialization to use activation_str from config for flexibility | code/train.py | Same issue as dropout — the activation function wasn't being read from the config, even though the models were already set up to accept it. | Passed activation_str=config["ACTIVATION"] explicitly into the model constructor call. | Found this right after the dropout issue, while double-checking that everything we'd made configurable in the models was actually wired up from the config file. |
| `01803c0` | Fix variable assignment for test_loader in data loading | code/train.py | get_loaders hands back three loaders — train, val, test — but they were being unpacked in the wrong order, so "test_loader" wasn't really the test set. | Corrected the tuple unpacking: train_loader, val_loader, test_loader = get_loaders(...). | Test results were coming out suspiciously close to validation results. Traced it back and the test loader was quietly pointing at the wrong data. |
| `8b38c77` | Add evaluation and logging of test loss and accuracy after training | code/train.py | Training would finish, but nothing ever actually ran the model against the test set or printed a result. | Added a call to trainer.evaluate(test_loader) after fit() completes, with a printed summary of final test loss and accuracy. | Ran through the whole pipeline start to finish and noticed it logged every epoch during training but never gave a final test number. |
| `5cdf371` | Fix data loading by squeezing labels for train, validation, and test datasets | code/data.py | The labels had an extra dimension tacked on that they shouldn't have, and the loss function didn't like that shape. | Added .squeeze() to train, validation, and test label tensors. | Got a shape mismatch the first time the loss function ran. Checked the label shapes and there was one dimension too many. |
| `a79d214` | Fix AlexNet initialization to accept num_classes parameter for dynamic output layer size | code/models.py, AlexNet.__init__ and AlexNet.classifier | AlexNet's constructor never actually took num_classes as an input, so its output layer was stuck at 11 classes no matter what dataset you ran it on. | Added num_classes as an explicit constructor parameter and replaced the hardcoded 11 with num_classes in the final nn.Linear layer. | While setting up the full run across all four datasets, noticed train.py was passing the class count into every model — except AlexNet had nowhere to put it. Checked and confirmed it really was outputting 11 classes even on datasets like chest, which only has 2. |

